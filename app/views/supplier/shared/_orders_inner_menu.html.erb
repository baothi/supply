<%
menu = [
    {
      section: Hingeto::Section::ORDERS,
      title: 'All Orders',
      link: supplier_orders_path,
      secret: false
    },
    {
        section: Hingeto::Section::REPORTED_ORDERS,
        title: 'Reported Orders',
        link: supplier_orders_reported_path,
        secret: true
    }
]
%>

<ul class="site-menu-sub">
  <% menu.each do |menu| %>
      <%= inner_menu_html(
              inner_menu: yield(:inner_menu),
              link: menu[:link],
              section: menu[:section],
              title: menu[:title],
              secret: menu[:secret],
              true_user: true_spree_user
          )
      %>
<% end %>
</ul>